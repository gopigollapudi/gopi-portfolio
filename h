# Initialize git repository
git init

# Add all your project files
git add .

# Commit your files
git commit -m "Initial commit - Cybersecurity portfolio website"

# Rename the main branch
git branch -M main

# Connect to your GitHub repository (replace YOUR-USERNAME)
git remote add origin https://github.com/YOUR-USERNAME/gopi-portfolio.git

# Push files to GitHub
git push -u origin main
